/**
 * Una pila funciona con el principio LIFO (Last In, First Out):
 * 👉 el último elemento que entra es el primero que sale.
 */

let ejemplo = [0,1,2,3,4,5,6,7];
ejemplo.push(8);
/* añade elementos al final push*/

let valor = ejemplo.pop();//elimina y devuelve el ultimo elemento del array  


/**
 * COLAS 
 * unshift() – Añadir elementos al principio
 * let ejemplo = [0,1,2,3,4,5,6,7];
 * ejemplo.unshift(-2, -1);   
 * 
 * Esto agrega -2 y -1 al inicio (en ese orden).
 * 
 * ✔ shift() – Quitar y devolver el primer elemento
 * let x = ejemplo.shift();
 * Esto elimina y devuelve el primer elemento de la cola.
 */

